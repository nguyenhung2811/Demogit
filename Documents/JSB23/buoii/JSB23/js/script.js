x = 5 

for ( let i = 1; i<= 100; i++) {
    // in cac so chan chia het cho 5 
    // i % 2 == 0: ktra i la so chan
    // i % 5 == 0: ktra i chia het cho 5
    // toán tử && chihr đúng khi cả 2 vế đều đúng
   if (i % 5 == 0 && i % 5 == 0) {
    console.log(i);
   }
}   